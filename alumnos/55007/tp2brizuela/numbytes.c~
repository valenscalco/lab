#include "histograma.h"
int bytesPorHijo(int ph,char* buffer,int leido){
	char copia[leido],*aux;	
	strcpy(copia,buffer);
	int i=0,j=0,bytes=2;
	aux=(char*)malloc(sizeof(char)*leido);
	while(j<=ph){
		aux[i]=copia[i];
		i++;
		if(copia[i]== ' '){
			j++;
		}
	}
return bytes;
}

