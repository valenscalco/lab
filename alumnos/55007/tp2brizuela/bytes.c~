#include "histograma.h"

int numBytes(int ph,char* buffer,int leido){	
	char *aux;	
	int j=0,caracter=0,newtam;
	aux=(char*)malloc(sizeof(char)*leido);
	while(j<ph){
		aux[caracter]=buffer[caracter];
		if(aux[caracter]== ' '){
			j++;
		}
		caracter++;
	}
	newtam=leido-caracter;
return newtam;
}
